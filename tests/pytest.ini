[pytest]
testpaths = unit security smoke
python_files = test_*.py
addopts = -v --tb=short --json-report --json-report-file=../test-reports/pytest-results.json
markers =
    smoke: smoke tests
    security: security-focused tests (auth, IDOR, prompt injection, etc.)
    live: requires TEST_MODE=live and live AWS access
