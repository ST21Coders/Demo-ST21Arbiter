"""Operator-facing scripts for the adversarial harness.

Modules in this package are executed via ``python3.13 -m scripts.<name>`` from
the harness root, so they can ``from src... import ...`` cleanly.
"""
