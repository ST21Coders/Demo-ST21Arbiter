# Where the SPA is served. In mock mode the Vite dev server starts automatically.
BASE_URL=http://localhost:5173

# 'mock' (default) = local Vite + USE_MOCK=true. 'live' = hit a real deployment.
TEST_MODE=mock

# Only needed when TEST_MODE=live (or for the @live Hosted UI test).
# Pull these from your team's CFN exports — see TESTING.md for the lookup command.
LIVE_BASE_URL=https://d5u0vv1zl3eqd.cloudfront.net
COGNITO_REGION=us-east-1
COGNITO_USER_POOL_ID=us-east-1_ZCn9RLdut
COGNITO_CLIENT_ID=7jj2t2ng0nta408g0h302f39ks
COGNITO_DOMAIN=poc-st21arbiter.auth.us-east-1.amazoncognito.com

# Dedicated test user — see TESTING.md for creating one. Never reuse a real demo user.
TEST_USER_USERNAME=e2e_test@arbiter.test
TEST_USER_PASSWORD=
